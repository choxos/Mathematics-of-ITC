# Rendered book output
/docs/
/_book/
/.quarto/
*_files/
# Quarto-generated HTML dependencies (third-party JS/CSS)
/site_libs/

# Copyrighted reference material (local only, do not distribute)
/documentation/refs/

# LaTeX intermediates
*.aux
*.log
*.toc
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
# Quarto book-level LaTeX artifact left in the project root by pdf render
/The-Mathematics-of-Indirect-Treatment-Comparisons.tex

# R / Stan
.Rhistory
.RData
.Rproj.user/
*.rds

# Draft review notes (local only, not part of the published book)
/feedback/

# OS / editor cruft
.DS_Store
*~
.vscode/
.idea/

**/*.quarto_ipynb

# Per-file HTML left in the source tree by an interrupted Quarto render
# (all published HTML belongs under docs/, which is ignored above)
/*.html
/chapters/**/*.html
/appendices/**/*.html
